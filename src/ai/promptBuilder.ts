import type { PageContext } from '../types/dom'

// Scope A — implement in promptBuilder.ts
export function buildPrompt(_transcript: string, _context: PageContext): string {
  throw new Error('Not implemented')
}
