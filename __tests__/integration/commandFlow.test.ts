import { describe, it } from 'vitest'

describe('command flow (integration)', () => {
  it.todo('full pipeline: transcript → DOM context → Claude → action → result')
  it.todo('destructive action triggers confirmation before execution')
  it.todo('low confidence transcript is rejected before calling Claude')
})
