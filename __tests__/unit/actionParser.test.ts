import { describe, it } from 'vitest'

describe('actionParser', () => {
  it.todo('parses a valid Claude JSON response into a BrowserAction')
  it.todo('throws on unparseable response')
  it.todo('throws on unknown action type')
})
