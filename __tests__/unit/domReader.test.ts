import { describe, it, expect } from 'vitest'

describe('domReader', () => {
  it.todo('extracts interactive elements from a simple page')
  it.todo('sanitizes script tags and event handlers')
  it.todo('never includes password field values')
  it.todo('truncates visible text to MAX_VISIBLE_TEXT_CHARS')
})
