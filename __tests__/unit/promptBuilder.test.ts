import { describe, it } from 'vitest'

describe('promptBuilder', () => {
  it.todo('includes page context in the prompt')
  it.todo('includes the user transcript in the prompt')
  it.todo('includes the system constraints in the prompt')
})
